import { SignInScreen } from "@/app/components/signin/SignInScreen";

/** Door variation 2 — the stacked mark leading the column, on
 *  the heading's own left edge.
 *
 *  Throwaway. Delete all four `signin-mock-*` routes once one of them wins. */
export default function Mock2Page() {
  return <SignInScreen variant="stacked" />;
}
