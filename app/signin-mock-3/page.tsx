import { SignInScreen } from "@/app/components/signin/SignInScreen";

/** Door variation 3 — the stacked mark and everything under it
 *  centred, as the page was before there was anything beside it.
 *
 *  Throwaway. Delete all four `signin-mock-*` routes once one of them wins. */
export default function Mock3Page() {
  return <SignInScreen variant="centred" />;
}
