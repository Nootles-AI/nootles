import { IntegrationsSettings } from "@/app/components/workspaces/settings/IntegrationsSettings";

/** What a workspace is connected to. */
export default function WorkspaceIntegrationsPage() {
  return <IntegrationsSettings />;
}
