import { MembersSettings } from "@/app/components/workspaces/settings/MembersSettings";

/** Who is in a workspace, and who has been asked. */
export default function WorkspaceMembersPage() {
  return <MembersSettings />;
}
