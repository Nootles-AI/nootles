import { AuditSettings } from "@/app/components/workspaces/settings/AuditSettings";

/** Who did what in a workspace, and when. Admins and owners. */
export default function WorkspaceAuditPage() {
  return <AuditSettings />;
}
