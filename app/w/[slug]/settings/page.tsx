import { GeneralSettings } from "@/app/components/workspaces/settings/GeneralSettings";

/** The first section of a workspace's settings: its name and address. */
export default function WorkspaceGeneralPage() {
  return <GeneralSettings />;
}
