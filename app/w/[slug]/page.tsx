import { WorkspaceHome } from "@/app/components/workspaces/WorkspaceHome";

/** A workspace's projects — what `/w/<slug>` opens on. */
export default function WorkspaceHomePage() {
  return <WorkspaceHome />;
}
